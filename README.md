# 🤖 مساعدك الطبي الذكي

تطبيق ذكي يستخدم تقنيات الذكاء الاصطناعي والمعالجة الطبيعية للغة لتقديم استشارات طبية موثوقة.

## ✨ المميزات

- 🏥 **استشارات طبية ذكية**: يجيب على الأسئلة الطبية بناءً على قاعدة معرفية شاملة
- 🌍 **دعم اللغة العربية**: واجهة كاملة باللغة العربية مع دعم RTL
- 🔍 **البحث الموسع**: يبحث عن أفضل المعلومات من قاعدة البيانات
- 💬 **محادثة طبيعية**: يقترح أسئلة متابعة ذكية
- 🎯 **دقة عالية**: يستخدم نماذج LLM متقدمة من Groq

## 🚀 خطوات التشغيل

### 1️⃣ إنشاء البيئة الافتراضية
```bash
python -m venv venv
```

### 2️⃣ تفعيل البيئة الافتراضية
```bash
# على Windows
.\venv\Scripts\activate

# على macOS/Linux
source venv/bin/activate
```

### 3️⃣ تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4️⃣ بناء قاعدة المعرفة
ضع ملفات البيانات (PDF أو TXT) في مجلد `data/` ثم قم بتشغيل:
```bash
python create_memory_for_llm.py
```

### 5️⃣ تشغيل التطبيق
```bash
streamlit run app.py
```

التطبيق سيفتح تلقائياً على: `http://localhost:8501`

## 📁 هيكل المشروع

```
project_sajeda/
├── app.py                      # التطبيق الرئيسي (Streamlit)
├── create_memory_for_llm.py    # بناء قاعدة المعرفة
├── connect_memory_with_llm.py  # اختبار الاتصال
├── requirements.txt            # المكتبات المطلوبة
├── .env                        # متغيرات البيئة (مفاتيح API)
├── data/                       # مجلد البيانات (ملفات PDF/TXT)
└── vectorstore/               # قاعدة بيانات FAISS
    └── db_faiss/              # ملفات قاعدة البيانات
```

## 🔑 متغيرات البيئة

يجب إضافة المفاتيح التالية في ملف `.env`:

```env
GROQ_API_KEY="your_groq_api_key_here"
HF_TOKEN="your_huggingface_token_here"
```

احصل على المفاتيح من:
- **Groq API**: https://console.groq.com
- **HuggingFace Token**: https://huggingface.co/settings/tokens

## 📚 إضافة بيانات جديدة

1. ضع ملفات PDF أو TXT في مجلد `data/`
2. قم بتشغيل: `python create_memory_for_llm.py`
3. أعد تشغيل التطبيق: `streamlit run app.py`

## 🛠️ المكتبات المستخدمة

- **Streamlit**: واجهة المستخدم
- **LangChain**: إدارة سلاسل المعالجة
- **Groq API**: نموذج اللغة الكبير
- **FAISS**: قاعدة بيانات المتجهات
- **HuggingFace**: نماذج التضمين

## 📝 الملاحظات

- تأكد من وجود اتصال إنترنت لتحميل النماذج
- قد يستغرق التشغيل الأول وقتاً لتحميل النماذج
- يمكنك تخصيص Prompt في `app.py` حسب احتياجاتك

## 🤝 المساهمة

يمكنك تحسين المشروع بـ:
- إضافة بيانات طبية أكثر
- تحسين الـ Prompt
- إضافة ميزات جديدة

---

**تم الإنشاء بواسطة**: فريق التطوير
**آخر تحديث**: 2025-11-30
